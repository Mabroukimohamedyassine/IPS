# IPS
This project implements a real‑time Intrusion Prevention System (IPS) capable of capturing network packets, extracting features, preprocessing them, and classifying each packet as malicious or benign using a machine‑learning model.
